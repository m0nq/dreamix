<div class="col-lg-12 text-center">
  <p id="notice"><%= notice %></p>

  <h1 style="font-family: Philosopher, Helvetica, sans; font-size: 3.5em;">Your Stream</h1>
  <%#
    Should be able to click on a song, (context menu pops up)
      + see more info about such as external link about the song,
      + play the song selected, thus reseting the randomized queue in the Stream
  %>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Artist</th>
        <th>Title</th>
        <th>Url</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @songs.each do |song| %>
        <tr>
          <td><%= song.artist %></td>
          <td><%= song.title %></td>
          <td><%= song.url %></td>
          <td><%= link_to 'Play', {:action => 'show', :id => song.id }, class: "btn btn-default btn-sm" %></td>
          <%# <td><%= link_to 'Edit', edit_song_path(song) %><%#</td>
          <td><%= link_to 'Destroy', song, method: :delete, data: { confirm: 'Are you sure?' } %><%#</td> %>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
  <%= link_to 'Back', songs_path, class: "btn btn-default btn-sm" %>
  <%# <%= link_to 'New Song', new_song_path %><%# <br> %>
</div>
